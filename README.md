# Hackathon Portal

## Repo Structure

- [`server`](./server/): backend

to run the system:

1, yarn install
2, docker-compose up -d 
3, yarn run db:migrate  
4, yarn dev  

- [`client`](./client/): frontend

to run the system:

1, yarn install
2, yarn dev
